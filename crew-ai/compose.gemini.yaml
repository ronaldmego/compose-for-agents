services:
  agents:
    environment:
      - LLM_PROVIDER=gemini
      - GEMINI_MODEL_NAME=gemini-1.5-flash-latest
    secrets:
      - gemini-api-key

secrets:
  gemini-api-key:
    file: secret.gemini-api-key
